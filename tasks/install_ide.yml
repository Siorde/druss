---
- hosts: all
  tasks:
  # Ajout des variables
  - name: Get the variables
    include_vars:
      file: "{{playbook_dir}}/../conf.yml"

  ##################################################################
  # Installation de VSCode
  ##################################################################
  # Telechagement du paquet .deb de VSCode
  - name: download vscdode.deb
    get_url:
      url: "https://go.microsoft.com/fwlink/?LinkID=760868"
      dest: "~{{ansible_user_id}}}/vscode.deb"
    when: ide == "vscode"

  # Installation de VSCode
  - name: install vscdode
    apt:
      deb: "~{{ansible_user_id}}}/vscode.deb"
    when: ide == "vscode"

  # Suppression de vscode.deb
  - name: remove vscode.deb
    file:
      state: absent
      path: "~{{ansible_user_id}}}/vscode.deb"